services:
  mind-journal-server:
    build: .
    env_file:
      - .env
    ports:
      - "80:8000"
    networks:
      - reverse_proxy_network
    restart: unless-stopped

networks:
  reverse_proxy_network:
    external: true
